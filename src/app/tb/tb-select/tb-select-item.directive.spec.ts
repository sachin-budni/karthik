import { TbSelectItemDirective } from './tb-select-item.directive';

describe('TbSelectItemDirective', () => {
  it('should create an instance', () => {
    const directive = new TbSelectItemDirective();
    expect(directive).toBeTruthy();
  });
});
