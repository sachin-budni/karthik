import { TbSelectGroupDirective } from './tb-select-group.directive';

describe('TbSelectGroupDirective', () => {
  it('should create an instance', () => {
    const directive = new TbSelectGroupDirective();
    expect(directive).toBeTruthy();
  });
});
